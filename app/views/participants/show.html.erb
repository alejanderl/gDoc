
<div  id="single-asset" class="span7 ofsset1">
<%= favourite_button(@participant) if current_user.present? %>

<p id="notice"><%= notice %></p>

<p>
  <span class="field-title"><%= t"global.participant" %></span>
  <h1><%= @participant.name %></h1>
</p>

<div class="image">
      <%= image_tag @participant.image_url(:thumb) if @participant.image? %>
    </div>
<p>
  <span class="field-title"><%=  t("misc.bio") %></span>
  <%= sanitize @participant.bio %>
</p>

<p>
  <span class="field-title">Tags:</span>
  <div>
      <%=  @participant.tag_list {|t| t} %>
  </div>
</p>

<p>
  <span class="field-title"><%=  t("misc.content_related") %></span>
  <%#= @photo.content_related %>
</p>



<%= edit_button @participant %>
<div class="clearfix"></div>
</div>
